syntax = "proto3";

package proto;

service SQLFlow {
  rpc Run(SQL) returns (Job) {}
  rpc ReadStderr(Job) returns (stream Line) {}
  rpc ReadStdout(Job) returns (stream Line) {}
}

message SQL {
  string sql = 1;
}

message Job {
  uint64 id = 1;
}

message Line {
  string line = 1;
}